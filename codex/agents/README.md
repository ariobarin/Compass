# Portable Subagents

Add each Codex subagent as `codex/agents/<name>.toml`, then add its name to
`codex.agents` in `manifests/portable-files.json`.

Compass installs selected subagents into the Codex home.
