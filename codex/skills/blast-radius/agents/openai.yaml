interface:
  display_name: "Blast Radius"
  short_description: "Prove what a change could break"
  default_prompt: "Use $blast-radius on the current change."
policy:
  allow_implicit_invocation: false
