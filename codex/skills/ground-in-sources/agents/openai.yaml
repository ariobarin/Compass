interface:
  display_name: "Ground in Sources"
  short_description: "Extract current and durable source philosophies"
  default_prompt: "Use $ground-in-sources to discover current official guidance and renowned original thinking, then extract and distill it into this artifact."
