# Portable Skills

Add each portable skill under `codex/skills/<name>/`, then add its name to
`agents.skills` in `manifests/portable-files.json`.

Compass installs selected skills into `$HOME/.agents/skills`.
